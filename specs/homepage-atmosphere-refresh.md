# Homepage Atmosphere Refresh

## Objective
Extend the cinematic homepage mood below the hero with subtle atmospheric gradients, improve the small value strip under the hero, and clean up the highlighted hero accent text.

## Requirements
- Keep the homepage section order and existing content structure.
- Add a shared atmospheric background treatment below the hero using restrained copper and cool-blue glows.
- Upgrade the value strip cards with richer surfaces and subtle gradient-border hover polish.
- Fix the `digital systems` hero accent text so it no longer shows muddy grey artifacts.
- Preserve readability, existing CTA destinations, and the current dark cinematic direction.

## Acceptance
- Homepage atmosphere continues below the hero without becoming noisy or overpowering the content.
- Value strip cards look more premium and hover cleanly.
- The hero accent text renders cleanly across device sizes.
- No new horizontal overflow appears.
- `npm run lint` passes.
- `npm run build` passes.
